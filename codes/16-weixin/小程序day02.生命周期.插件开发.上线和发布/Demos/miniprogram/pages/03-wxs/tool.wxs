// tool.js

      // 每一个wxs里面都有一个module，用module，把ml_slice函数导出去
      // 截取
      function ml_slice(str) {
        return str.slice(1)
      }

      // 用module，把ml_slice函数导出去
      module.exports.ml_slice = ml_slice

